mutation UploadFile($file: Upload!, $folderId: String, $totalChunks: Int, $chunkNumber: Int) {
  uploadFile(file: $file, folderId: $folderId, totalChunks: $totalChunks, chunkNumber: $chunkNumber) {
    file_name,
    type,
    id
  }
}